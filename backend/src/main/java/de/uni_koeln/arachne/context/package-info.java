/**
 * This package contains classes needed for context handling and retrieval. A context is an objects whose information is stored in another table
 * than the table the queried object belongs to or even outside of the database. Some contexts are integral parts of an objects description like
 * Ort, Literatur, etc. Others are independent objects that are associated with the queried object. 
 */
package de.uni_koeln.arachne.context;