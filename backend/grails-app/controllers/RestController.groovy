/**
 * Base class for all Grails controllers which RESTfully handle the http requests.
 * Request mappings are configured in the default package in UrlMappings.groovy.
 * <br>
 * This is only a dummy class to work around groovydocs problem with grails classes outside the default package.
 */


class RestController {
}
